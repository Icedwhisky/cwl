#include "seqlist.h"

int main()
{
	SeqList		L;
	int			p;
	int			q;
	ElemType	*e;
	
	
	printf("开始初始化顺序表...\n"); 
	InitList(&L);
	

//	printf("开始查找顺序表元素.\n");
//	printf("请输入要查找的元素值:\n");
//	scanf("%d",&q);
//	p=Locate(L,q);
//	if(p == -1)
//		printf("在此线性表中没有该元素!\n");
//	else
//		printf("该元素在线性表中的位置为:%d\n",p); 
	
	
//	printf("开始插入元素.\n"); 
//	InsList(&L, 2, 3);
	
	
//	e = (ElemType*)malloc(sizeof(ElemType));
//	printf("开始删除元素.\n"); 
//	DelList(&L, 2, e);
//	printf("%d\n", *e);
	
		
	return 0;
}
